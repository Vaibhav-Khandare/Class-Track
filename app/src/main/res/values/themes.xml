<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ClassTrack" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ClassTrack" parent="Base.Theme.ClassTrack" />
    <style name="Theme.ClassTrack.Splash" parent="Theme.SplashScreen">

        <!-- Background -->
        <item name="windowSplashScreenBackground">@drawable/splash_bg</item>

        <!-- Logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_class_track</item>

        <!-- Icon background -->
        <item name="windowSplashScreenIconBackgroundColor">#00000000</item>

        <!-- After splash -->
        <item name="postSplashScreenTheme">@style/Theme.ClassTrack</item>

    </style>

</resources>